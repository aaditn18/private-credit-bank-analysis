"""Agent package.

``loop`` depends on DB modules; import it directly (``from
pc_analyst.agent.loop import AgentLoop``) rather than via this package
to keep the lightweight ``decomposer`` importable in isolation.
"""
